// src/helpers/enviarDato.js

const enviarDato = (dato) => {
    console.log("Enviando dato:", dato);
    // Aquí puedes agregar la lógica para manejar el dato recibido
};

export default enviarDato;
